import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    texto: {
        fontSize: '30px',
        color: 'White',
        fontWeight: 'bold'
    }
})

export default styles;
